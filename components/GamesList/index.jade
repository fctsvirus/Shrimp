index:
  .GamesList
    .u-textDecoration
      | Games:
    each gamesList as #game
      .GamesList-data
        .GamesList-data-gameName
          | {{#game.name}}
        if #game.canJoin
          a.GamesList-data-a.u-textAlign(on-click='join(#game, $event)') Join